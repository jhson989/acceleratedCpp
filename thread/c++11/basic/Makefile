CC = g++
INCS = 
OBJS = 
MAIN = main.cpp 
OPTION =  -O0 -lpthread
PROGRAM = thread


${PROGRAM} : ${MAIN} ${OBJS} ${INCS} Makefile
	${CC} -o ${PROGRAM} ${MAIN} ${OBJS} ${OPTION}

clean : 
	rm ${PROGRAM}

